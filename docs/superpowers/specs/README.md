# Specs

每篇 spec 以日期命名，是 **当时** 的设计快照。一旦合入就不再回写，
反映的是写作那一刻团队达成的共识，**不是当前系统的真实形态**。

如果想了解当前架构与契约，先看：

- `/packages/contract/src/contract.ts` — 基座 ⇄ 模块契约的单一事实来源
- `/CLAUDE.md`（workspace 根） — 跨子项目协作规则

旧 spec 里可能出现已被取代的包名 / 接口名 / 字段名，遇到不要按字面查找代码，
以契约包当前形态为准。
