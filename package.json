{
  "name": "boundary-desktop",
  "private": true,
  "packageManager": "pnpm@11.4.0",
  "scripts": {
    "dev": "pnpm -F @boundary-desktop/shell dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "build:mods": "node scripts/build-modules.mjs",
    "pack:mods": "node scripts/pack-modules.mjs",
    "clean": "node scripts/clean.mjs"
  },
  "devDependencies": {
    "electron": "^42.3.0",
    "esbuild": "^0.21.5",
    "typescript": "^6.0.3"
  }
}
